# SecureContent AI — gitleaks ignore
# All remaining synthetic secrets are allowlisted via .gitleaks.toml (paths + regexes).
# Keep this file minimal — add fingerprints only via `gitleaks detect` when needed.
# See: https://github.com/gitleaks/gitleaks#gitleaksignore
